name: Test schedule

on:
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Mostrar fecha
        run: |
          echo "Evento: ${{ github.event_name }}"
          date -u
